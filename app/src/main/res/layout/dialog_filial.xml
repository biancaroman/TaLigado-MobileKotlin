<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp"
    android:gravity="start">

    <!-- Imagem da filial -->
    <ImageView
        android:id="@+id/dialogImgFilial"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:contentDescription="@string/todo"
        android:scaleType="centerCrop"
        android:layout_marginBottom="20dp"/>

    <ImageView
        android:id="@+id/iconEditarFilial"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_edit"
        android:layout_gravity="end"
        android:layout_alignParentEnd="true"
        android:layout_marginEnd="16dp"/>

    <!-- Nome da filial -->
    <TextView
        android:id="@+id/dialogNomeFilial"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/nome_da_filial"
        android:textColor="@color/black"
        android:textSize="18sp"
        android:layout_marginTop="8dp"/>


    <!-- Endereço da filial -->
    <TextView
        android:id="@+id/dialogEnderecoFilial"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Endereco"
        android:textColor="@color/black"
        android:textSize="16sp"
        android:layout_marginTop="8dp"/>

    <!-- Descrição da filial -->
    <TextView
        android:id="@+id/dialogDescricaoFilial"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Descricao"
        android:textColor="@color/black"
        android:textSize="16sp"
        android:layout_marginTop="8dp"/>

    <!-- Botões -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:layout_marginTop="16dp">

        <!-- Botão de Remover Filial -->
        <Button
            android:id="@+id/btnRemoverFilial"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Remover Filial"
            android:textColor="@color/black" />

        <!-- Botão de Ver Detalhes -->
        <Button
            android:id="@+id/btnVerDetalhesFilial"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Ver Detalhes"
            android:textColor="@color/black"/>
    </LinearLayout>
</LinearLayout>
